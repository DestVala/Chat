package com.server.umm.aplication.packet.message;

public class EmptyType extends BaseMessage {
    /**
     * Instance of class resolve null pointer exception with empty stream,
     * because stream sometimes fill tcp packet by zero.
     * */
}
